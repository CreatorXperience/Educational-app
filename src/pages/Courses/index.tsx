import styled from "styled-components";

const Courses = () => {
  return (
    <CoursesWrapper>
      <div>Courses</div>
    </CoursesWrapper>
  );
};

export default Courses;

const CoursesWrapper = styled.div``;
